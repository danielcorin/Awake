// Generated by AppAutomation 1.0.0 from API/openapi.yaml. Do not edit.
import Foundation
import AutomationHTTP
import OpenAPIRuntime
import AwakeCore

struct GeneratedHTTPBridge: APIProtocol {
    let client: AutomationClient
    let transfers: TransferStore
    func configGet(_ input: Operations.configGet.Input) async throws -> Operations.configGet.Output {
        do {
            var values: [String: JSONValue] = [:]
            values["key"] = try JSONValue.encode(input.path.key)
            let payload = JSONValue.object(values)
            try APIOperations.ConfigGet.definition.validate(payload)
            let result = try await client.call(APIOperations.ConfigGet.self, input: payload.decode())
            let body: Components.Schemas.ConfigGetResult = try JSONValue.encode(result).decode()
            return .ok(.init(body: .json(body)))
        } catch {
            let failure = AutomationFailure.normalize(error)
            let body: Components.Schemas.ErrorResult = try JSONValue.encode(OperationError(requestId: AutomationContext.requestId, error: failure)).decode()
            return .default(statusCode: failure.httpStatus, .init(body: .json(body)))
        }
    }
    func configList(_ input: Operations.configList.Input) async throws -> Operations.configList.Output {
        do {
            var values: [String: JSONValue] = [:]
            if let value = input.query.all { values["all"] = try JSONValue.encode(value) }
            let payload = JSONValue.object(values)
            try APIOperations.ConfigList.definition.validate(payload)
            let result = try await client.call(APIOperations.ConfigList.self, input: payload.decode())
            let body: Components.Schemas.ConfigListResult = try JSONValue.encode(result).decode()
            return .ok(.init(body: .json(body)))
        } catch {
            let failure = AutomationFailure.normalize(error)
            let body: Components.Schemas.ErrorResult = try JSONValue.encode(OperationError(requestId: AutomationContext.requestId, error: failure)).decode()
            return .default(statusCode: failure.httpStatus, .init(body: .json(body)))
        }
    }
    func configReload(_ input: Operations.configReload.Input) async throws -> Operations.configReload.Output {
        do {
            let values: [String: JSONValue] = [:]
            let payload = JSONValue.object(values)
            try APIOperations.ConfigReload.definition.validate(payload)
            let result = try await client.call(APIOperations.ConfigReload.self, input: payload.decode())
            let body: Components.Schemas.ConfigReloadResult = try JSONValue.encode(result).decode()
            return .ok(.init(body: .json(body)))
        } catch {
            let failure = AutomationFailure.normalize(error)
            let body: Components.Schemas.ErrorResult = try JSONValue.encode(OperationError(requestId: AutomationContext.requestId, error: failure)).decode()
            return .default(statusCode: failure.httpStatus, .init(body: .json(body)))
        }
    }
    func configSet(_ input: Operations.configSet.Input) async throws -> Operations.configSet.Output {
        do {
            var values: [String: JSONValue] = [:]
            values["key"] = try JSONValue.encode(input.path.key)
            for (key, value) in HTTPRequestContext.body?.object ?? [:] {
                guard values[key] == nil else { throw AutomationFailure("invalid_input", "A body field duplicates a path/query parameter.") }
                values[key] = value
            }
            let payload = JSONValue.object(values)
            try APIOperations.ConfigSet.definition.validate(payload)
            let result = try await client.call(APIOperations.ConfigSet.self, input: payload.decode())
            let body: Components.Schemas.ConfigSetResult = try JSONValue.encode(result).decode()
            return .ok(.init(body: .json(body)))
        } catch {
            let failure = AutomationFailure.normalize(error)
            let body: Components.Schemas.ErrorResult = try JSONValue.encode(OperationError(requestId: AutomationContext.requestId, error: failure)).decode()
            return .default(statusCode: failure.httpStatus, .init(body: .json(body)))
        }
    }
    func configUnset(_ input: Operations.configUnset.Input) async throws -> Operations.configUnset.Output {
        do {
            var values: [String: JSONValue] = [:]
            values["key"] = try JSONValue.encode(input.path.key)
            let payload = JSONValue.object(values)
            try APIOperations.ConfigUnset.definition.validate(payload)
            let result = try await client.call(APIOperations.ConfigUnset.self, input: payload.decode())
            let body: Components.Schemas.ConfigUnsetResult = try JSONValue.encode(result).decode()
            return .ok(.init(body: .json(body)))
        } catch {
            let failure = AutomationFailure.normalize(error)
            let body: Components.Schemas.ErrorResult = try JSONValue.encode(OperationError(requestId: AutomationContext.requestId, error: failure)).decode()
            return .default(statusCode: failure.httpStatus, .init(body: .json(body)))
        }
    }
    func configValidate(_ input: Operations.configValidate.Input) async throws -> Operations.configValidate.Output {
        do {
            var values: [String: JSONValue] = [:]
            for (key, value) in HTTPRequestContext.body?.object ?? [:] {
                guard values[key] == nil else { throw AutomationFailure("invalid_input", "A body field duplicates a path/query parameter.") }
                values[key] = value
            }
            let payload = JSONValue.object(values)
            try APIOperations.ConfigValidate.definition.validate(payload)
            let result = try await client.call(APIOperations.ConfigValidate.self, input: payload.decode())
            let body: Components.Schemas.ConfigValidateResult = try JSONValue.encode(result).decode()
            return .ok(.init(body: .json(body)))
        } catch {
            let failure = AutomationFailure.normalize(error)
            let body: Components.Schemas.ErrorResult = try JSONValue.encode(OperationError(requestId: AutomationContext.requestId, error: failure)).decode()
            return .default(statusCode: failure.httpStatus, .init(body: .json(body)))
        }
    }
    func quit(_ input: Operations.quit.Input) async throws -> Operations.quit.Output {
        do {
            let values: [String: JSONValue] = [:]
            let payload = JSONValue.object(values)
            try APIOperations.Quit.definition.validate(payload)
            let result = try await client.call(APIOperations.Quit.self, input: payload.decode())
            let body: Components.Schemas.QuitResult = try JSONValue.encode(result).decode()
            return .ok(.init(body: .json(body)))
        } catch {
            let failure = AutomationFailure.normalize(error)
            let body: Components.Schemas.ErrorResult = try JSONValue.encode(OperationError(requestId: AutomationContext.requestId, error: failure)).decode()
            return .default(statusCode: failure.httpStatus, .init(body: .json(body)))
        }
    }
    func show(_ input: Operations.show.Input) async throws -> Operations.show.Output {
        do {
            let values: [String: JSONValue] = [:]
            let payload = JSONValue.object(values)
            try APIOperations.Show.definition.validate(payload)
            let result = try await client.call(APIOperations.Show.self, input: payload.decode())
            let body: Components.Schemas.ShowResult = try JSONValue.encode(result).decode()
            return .ok(.init(body: .json(body)))
        } catch {
            let failure = AutomationFailure.normalize(error)
            let body: Components.Schemas.ErrorResult = try JSONValue.encode(OperationError(requestId: AutomationContext.requestId, error: failure)).decode()
            return .default(statusCode: failure.httpStatus, .init(body: .json(body)))
        }
    }
    func status(_ input: Operations.status.Input) async throws -> Operations.status.Output {
        do {
            let values: [String: JSONValue] = [:]
            let payload = JSONValue.object(values)
            try APIOperations.Status.definition.validate(payload)
            let result = try await client.call(APIOperations.Status.self, input: payload.decode())
            let body: Components.Schemas.StatusResult = try JSONValue.encode(result).decode()
            return .ok(.init(body: .json(body)))
        } catch {
            let failure = AutomationFailure.normalize(error)
            let body: Components.Schemas.ErrorResult = try JSONValue.encode(OperationError(requestId: AutomationContext.requestId, error: failure)).decode()
            return .default(statusCode: failure.httpStatus, .init(body: .json(body)))
        }
    }
    func wakeOff(_ input: Operations.wakeOff.Input) async throws -> Operations.wakeOff.Output {
        do {
            let values: [String: JSONValue] = [:]
            let payload = JSONValue.object(values)
            try APIOperations.WakeOff.definition.validate(payload)
            let result = try await client.call(APIOperations.WakeOff.self, input: payload.decode())
            let body: Components.Schemas.WakeStateResult = try JSONValue.encode(result).decode()
            return .ok(.init(body: .json(body)))
        } catch {
            let failure = AutomationFailure.normalize(error)
            let body: Components.Schemas.ErrorResult = try JSONValue.encode(OperationError(requestId: AutomationContext.requestId, error: failure)).decode()
            return .default(statusCode: failure.httpStatus, .init(body: .json(body)))
        }
    }
    func wakeOn(_ input: Operations.wakeOn.Input) async throws -> Operations.wakeOn.Output {
        do {
            var values: [String: JSONValue] = [:]
            for (key, value) in HTTPRequestContext.body?.object ?? [:] {
                guard values[key] == nil else { throw AutomationFailure("invalid_input", "A body field duplicates a path/query parameter.") }
                values[key] = value
            }
            let payload = JSONValue.object(values)
            try APIOperations.WakeOn.definition.validate(payload)
            let result = try await client.call(APIOperations.WakeOn.self, input: payload.decode())
            let body: Components.Schemas.WakeStateResult = try JSONValue.encode(result).decode()
            return .ok(.init(body: .json(body)))
        } catch {
            let failure = AutomationFailure.normalize(error)
            let body: Components.Schemas.ErrorResult = try JSONValue.encode(OperationError(requestId: AutomationContext.requestId, error: failure)).decode()
            return .default(statusCode: failure.httpStatus, .init(body: .json(body)))
        }
    }
    func wakeState(_ input: Operations.wakeState.Input) async throws -> Operations.wakeState.Output {
        do {
            let values: [String: JSONValue] = [:]
            let payload = JSONValue.object(values)
            try APIOperations.WakeState.definition.validate(payload)
            let result = try await client.call(APIOperations.WakeState.self, input: payload.decode())
            let body: Components.Schemas.WakeStateResult = try JSONValue.encode(result).decode()
            return .ok(.init(body: .json(body)))
        } catch {
            let failure = AutomationFailure.normalize(error)
            let body: Components.Schemas.ErrorResult = try JSONValue.encode(OperationError(requestId: AutomationContext.requestId, error: failure)).decode()
            return .default(statusCode: failure.httpStatus, .init(body: .json(body)))
        }
    }
}
