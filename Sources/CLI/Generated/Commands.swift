// Generated by AppAutomation 1.0.0 from API/openapi.yaml. Do not edit.
import Foundation
import AutomationCLI
import AwakeCore

enum GeneratedCLI {
    struct ConfigGet: AsyncParsableCommand {
        static let configuration = CommandConfiguration(commandName: "get", abstract: "Config get")
        @Argument(help: "key (string)") var argument0: String?
        @Flag(help: "Emit a structured JSON result.") var json = false
        @Option(name: .customLong("input-file"), help: "Complete input JSON file; \'-\' reads stdin. Cannot combine with field flags.") var inputFile: String?
        @Option(help: "Set a nullable field to null. Repeat for multiple fields.") var clear: [String] = []
        mutating func run() async throws {
            var values: [String: String] = [:]
            values["key"] = argument0
            try await CLIEnvironment.current.run(APIOperations.ConfigGet.self, values: values, clear: clear, inputFile: inputFile, json: json, uploadFile: nil, outputFile: nil, overwrite: false)
        }
    }
    struct ConfigList: AsyncParsableCommand {
        static let configuration = CommandConfiguration(commandName: "list", abstract: "Config list")
        @Option(name: .customLong("all"), help: "all (boolean)") var value_all: String?
        @Flag(help: "Emit a structured JSON result.") var json = false
        @Option(name: .customLong("input-file"), help: "Complete input JSON file; \'-\' reads stdin. Cannot combine with field flags.") var inputFile: String?
        @Option(help: "Set a nullable field to null. Repeat for multiple fields.") var clear: [String] = []
        mutating func run() async throws {
            var values: [String: String] = [:]
            values["all"] = value_all
            try await CLIEnvironment.current.run(APIOperations.ConfigList.self, values: values, clear: clear, inputFile: inputFile, json: json, uploadFile: nil, outputFile: nil, overwrite: false)
        }
    }
    struct ConfigReload: AsyncParsableCommand {
        static let configuration = CommandConfiguration(commandName: "reload", abstract: "Config reload")
        @Flag(help: "Emit a structured JSON result.") var json = false
        @Option(name: .customLong("input-file"), help: "Complete input JSON file; \'-\' reads stdin. Cannot combine with field flags.") var inputFile: String?
        @Option(help: "Set a nullable field to null. Repeat for multiple fields.") var clear: [String] = []
        mutating func run() async throws {
            let values: [String: String] = [:]
            try await CLIEnvironment.current.run(APIOperations.ConfigReload.self, values: values, clear: clear, inputFile: inputFile, json: json, uploadFile: nil, outputFile: nil, overwrite: false)
        }
    }
    struct ConfigSet: AsyncParsableCommand {
        static let configuration = CommandConfiguration(commandName: "set", abstract: "Config set")
        @Argument(help: "key (string)") var argument0: String?
        @Option(name: .customLong("value"), help: "value (string); required") var value_value: String?
        @Flag(help: "Emit a structured JSON result.") var json = false
        @Option(name: .customLong("input-file"), help: "Complete input JSON file; \'-\' reads stdin. Cannot combine with field flags.") var inputFile: String?
        @Option(help: "Set a nullable field to null. Repeat for multiple fields.") var clear: [String] = []
        mutating func run() async throws {
            var values: [String: String] = [:]
            values["key"] = argument0
            values["value"] = value_value
            try await CLIEnvironment.current.run(APIOperations.ConfigSet.self, values: values, clear: clear, inputFile: inputFile, json: json, uploadFile: nil, outputFile: nil, overwrite: false)
        }
    }
    struct ConfigUnset: AsyncParsableCommand {
        static let configuration = CommandConfiguration(commandName: "unset", abstract: "Config unset")
        @Argument(help: "key (string)") var argument0: String?
        @Flag(help: "Emit a structured JSON result.") var json = false
        @Option(name: .customLong("input-file"), help: "Complete input JSON file; \'-\' reads stdin. Cannot combine with field flags.") var inputFile: String?
        @Option(help: "Set a nullable field to null. Repeat for multiple fields.") var clear: [String] = []
        mutating func run() async throws {
            var values: [String: String] = [:]
            values["key"] = argument0
            try await CLIEnvironment.current.run(APIOperations.ConfigUnset.self, values: values, clear: clear, inputFile: inputFile, json: json, uploadFile: nil, outputFile: nil, overwrite: false)
        }
    }
    struct ConfigValidate: AsyncParsableCommand {
        static let configuration = CommandConfiguration(commandName: "validate", abstract: "Config validate")
        @Option(name: .customLong("content"), help: "content (string)") var value_content: String?
        @Flag(help: "Emit a structured JSON result.") var json = false
        @Option(name: .customLong("input-file"), help: "Complete input JSON file; \'-\' reads stdin. Cannot combine with field flags.") var inputFile: String?
        @Option(help: "Set a nullable field to null. Repeat for multiple fields.") var clear: [String] = []
        mutating func run() async throws {
            var values: [String: String] = [:]
            values["content"] = value_content
            try await CLIEnvironment.current.run(APIOperations.ConfigValidate.self, values: values, clear: clear, inputFile: inputFile, json: json, uploadFile: nil, outputFile: nil, overwrite: false)
        }
    }
    struct Quit: AsyncParsableCommand {
        static let configuration = CommandConfiguration(commandName: "quit", abstract: "Quit")
        @Flag(help: "Emit a structured JSON result.") var json = false
        @Option(name: .customLong("input-file"), help: "Complete input JSON file; \'-\' reads stdin. Cannot combine with field flags.") var inputFile: String?
        @Option(help: "Set a nullable field to null. Repeat for multiple fields.") var clear: [String] = []
        mutating func run() async throws {
            let values: [String: String] = [:]
            try await CLIEnvironment.current.run(APIOperations.Quit.self, values: values, clear: clear, inputFile: inputFile, json: json, uploadFile: nil, outputFile: nil, overwrite: false)
        }
    }
    struct Show: AsyncParsableCommand {
        static let configuration = CommandConfiguration(commandName: "show", abstract: "Show")
        @Flag(help: "Emit a structured JSON result.") var json = false
        @Option(name: .customLong("input-file"), help: "Complete input JSON file; \'-\' reads stdin. Cannot combine with field flags.") var inputFile: String?
        @Option(help: "Set a nullable field to null. Repeat for multiple fields.") var clear: [String] = []
        mutating func run() async throws {
            let values: [String: String] = [:]
            try await CLIEnvironment.current.run(APIOperations.Show.self, values: values, clear: clear, inputFile: inputFile, json: json, uploadFile: nil, outputFile: nil, overwrite: false)
        }
    }
    struct Status: AsyncParsableCommand {
        static let configuration = CommandConfiguration(commandName: "status", abstract: "Status")
        @Flag(help: "Emit a structured JSON result.") var json = false
        @Option(name: .customLong("input-file"), help: "Complete input JSON file; \'-\' reads stdin. Cannot combine with field flags.") var inputFile: String?
        @Option(help: "Set a nullable field to null. Repeat for multiple fields.") var clear: [String] = []
        mutating func run() async throws {
            let values: [String: String] = [:]
            try await CLIEnvironment.current.run(APIOperations.Status.self, values: values, clear: clear, inputFile: inputFile, json: json, uploadFile: nil, outputFile: nil, overwrite: false)
        }
    }
    struct WakeOff: AsyncParsableCommand {
        static let configuration = CommandConfiguration(commandName: "off", abstract: "Stop keeping the Mac awake")
        @Flag(help: "Emit a structured JSON result.") var json = false
        @Option(name: .customLong("input-file"), help: "Complete input JSON file; \'-\' reads stdin. Cannot combine with field flags.") var inputFile: String?
        @Option(help: "Set a nullable field to null. Repeat for multiple fields.") var clear: [String] = []
        mutating func run() async throws {
            let values: [String: String] = [:]
            try await CLIEnvironment.current.run(APIOperations.WakeOff.self, values: values, clear: clear, inputFile: inputFile, json: json, uploadFile: nil, outputFile: nil, overwrite: false)
        }
    }
    struct WakeOn: AsyncParsableCommand {
        static let configuration = CommandConfiguration(commandName: "on", abstract: "Start keeping the Mac awake")
        @Option(name: .customLong("minutes"), help: "Session length in minutes, 0 through 1440; zero means indefinite. Omitted uses default-duration-minutes.") var value_durationMinutes: String?
        @Option(name: .customLong("display"), help: "keepDisplayOn (boolean)") var value_keepDisplayOn: String?
        @Option(name: .customLong("disk"), help: "preventDiskIdle (boolean)") var value_preventDiskIdle: String?
        @Option(name: .customLong("system"), help: "preventSystemSleep (boolean)") var value_preventSystemSleep: String?
        @Flag(help: "Emit a structured JSON result.") var json = false
        @Option(name: .customLong("input-file"), help: "Complete input JSON file; \'-\' reads stdin. Cannot combine with field flags.") var inputFile: String?
        @Option(help: "Set a nullable field to null. Repeat for multiple fields.") var clear: [String] = []
        mutating func run() async throws {
            var values: [String: String] = [:]
            values["durationMinutes"] = value_durationMinutes
            values["keepDisplayOn"] = value_keepDisplayOn
            values["preventDiskIdle"] = value_preventDiskIdle
            values["preventSystemSleep"] = value_preventSystemSleep
            try await CLIEnvironment.current.run(APIOperations.WakeOn.self, values: values, clear: clear, inputFile: inputFile, json: json, uploadFile: nil, outputFile: nil, overwrite: false)
        }
    }
    struct WakeState: AsyncParsableCommand {
        static let configuration = CommandConfiguration(commandName: "state", abstract: "Report the current wake session")
        @Flag(help: "Emit a structured JSON result.") var json = false
        @Option(name: .customLong("input-file"), help: "Complete input JSON file; \'-\' reads stdin. Cannot combine with field flags.") var inputFile: String?
        @Option(help: "Set a nullable field to null. Repeat for multiple fields.") var clear: [String] = []
        mutating func run() async throws {
            let values: [String: String] = [:]
            try await CLIEnvironment.current.run(APIOperations.WakeState.self, values: values, clear: clear, inputFile: inputFile, json: json, uploadFile: nil, outputFile: nil, overwrite: false)
        }
    }
    struct ConfigGroup: AsyncParsableCommand {
        static var configuration: CommandConfiguration { .init(commandName: "config", subcommands: [
            CLIEnvironment.current.command(APIOperations.ConfigGet.self, default: ConfigGet.self),
            CLIEnvironment.current.command(APIOperations.ConfigList.self, default: ConfigList.self),
            CLIEnvironment.current.command(APIOperations.ConfigReload.self, default: ConfigReload.self),
            CLIEnvironment.current.command(APIOperations.ConfigSet.self, default: ConfigSet.self),
            CLIEnvironment.current.command(APIOperations.ConfigUnset.self, default: ConfigUnset.self),
            CLIEnvironment.current.command(APIOperations.ConfigValidate.self, default: ConfigValidate.self)
        ]) }
    }
    static var commands: [ParsableCommand.Type] { [
        CLIEnvironment.current.command(APIOperations.Quit.self, default: Quit.self),
        CLIEnvironment.current.command(APIOperations.Show.self, default: Show.self),
        CLIEnvironment.current.command(APIOperations.Status.self, default: Status.self),
        CLIEnvironment.current.command(APIOperations.WakeOff.self, default: WakeOff.self),
        CLIEnvironment.current.command(APIOperations.WakeOn.self, default: WakeOn.self),
        CLIEnvironment.current.command(APIOperations.WakeState.self, default: WakeState.self),
        ConfigGroup.self
    ] }
}
